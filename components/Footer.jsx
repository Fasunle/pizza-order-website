import Image from "next/image";

const styles = {
  footer: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
};

const Footer = () => {
  return <footer style={styles.footer}>Powered by Kehinde Fasunle</footer>;
};

export default Footer;
